{
  "name": "groepscore",
  "version": "0.1.0",
  "description": "Puntensysteem en leaderboard voor een community (onder constructie)",
  "type": "module",
  "private": true,
"scripts": {
  "test": "node --import tsx src/test.ts",
  "leaderboard": "node --import tsx src/leaderboard.ts",
  "export": "node --import tsx src/export.ts",
  "reset:month": "node --import tsx src/monthlyReset.ts",
  "simulate:spam": "node --import tsx src/simulateSpam.ts"
}
,
  "dependencies": {
    "better-sqlite3": "^9.4.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0",
    "typescript": "^5.3.3"
  }
}
